{
  "name": "hyperscaled-extension",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:integration": "vitest run --config vitest.config.integration.js",
    "test:integration:watch": "vitest --config vitest.config.integration.js",
    "test:all": "vitest run && vitest run --config vitest.config.integration.js"
  },
  "devDependencies": {
    "@msgpack/msgpack": "^3.1.3",
    "ethers": "^6.16.0",
    "vitest": "^2.1.0"
  }
}
